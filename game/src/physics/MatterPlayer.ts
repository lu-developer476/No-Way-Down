import Phaser from 'phaser';import { CollisionCategory,CollisionMask } from './CollisionCategories';import { GroundContactTracker } from './GroundContactTracker';
export class MatterPlayer {readonly sprite:Phaser.Physics.Matter.Sprite;readonly ground=new GroundContactTracker();private cursors:Phaser.Types.Input.Keyboard.CursorKeys;private jumpBufferedUntil=0;private groundedUntil=0;constructor(private scene:Phaser.Scene,x:number,y:number){this.sprite=scene.matter.add.sprite(x,y,'nwd-characters',undefined,{label:'player',friction:0.08,frictionAir:0.04,restitution:0,collisionFilter:{category:CollisionCategory.Player,mask:CollisionMask.Player}}).setRectangle(38,68).setDisplaySize(38,68).setFixedRotation();this.cursors=scene.input.keyboard!.createCursorKeys()}
 update(time:number){const vx=this.sprite.body?.velocity.x??0;const target=this.cursors.left.isDown?-4.5:this.cursors.right.isDown?4.5:0;this.sprite.setVelocityX(Phaser.Math.Linear(vx,target,target===0?.22:.16));if(this.cursors.up.isDown||this.cursors.space.isDown)this.jumpBufferedUntil=time+120;const grounded=this.ground.grounded||time<this.groundedUntil;if(grounded)this.groundedUntil=time+100;if(this.jumpBufferedUntil>=time&&grounded){this.sprite.setVelocityY(-10.5);this.jumpBufferedUntil=0;this.ground.reset()}this.sprite.setFlipX(target<0)}
 destroy(){this.sprite.destroy();this.ground.reset()}}
