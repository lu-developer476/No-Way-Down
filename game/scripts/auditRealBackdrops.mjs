import fs from'node:fs';import path from'node:path';import process from'node:process';const repo=path.resolve(import.meta.dirname,'../..');const source=fs.readFileSync(path.join(repo,'game/src/config/LevelBackdropProfiles.ts'),'utf8');const ids=[...source.matchAll(/runtimeLevelId:'([^']+)'/g)].map(m=>m[1]);const assets=[...source.matchAll(/backgroundAssetPath:'\/([^']+)'/g)].map(m=>m[1]);const failures=[];if(ids.length!==10||new Set(ids).size!==10)failures.push('se requieren 10 perfiles únicos');for(const asset of assets)if(!fs.existsSync(path.join(repo,'public',asset)))failures.push(`asset inexistente: ${asset}`);if(/blur:true/.test(source))failures.push('blur habilitado');const lighting=fs.readFileSync(path.join(repo,'game/src/visual/SurvivalLightingSystem.ts'),'utf8');if(/fillEllipse/.test(lighting))failures.push('elipse gigante presente');for(const forbidden of ['visual-v3','visual-v4','BackdropSystemV3','PhotoRendererV2'])if(source.includes(forbidden))failures.push(forbidden);if(failures.length){console.error(failures.join('\n'));process.exit(1)}console.log(`Real backdrops OK: ${ids.length} profiles, ${new Set(assets).size} existing photographs`);
