import Phaser from 'phaser';export class UIScene extends Phaser.Scene{constructor(){super('UIScene')}create(){this.add.text(16,16,'NO WAY DOWN',{fontFamily:'monospace',fontSize:'14px',color:'#ffffff'}).setScrollFactor(0)}}
