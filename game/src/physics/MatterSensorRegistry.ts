export class MatterSensorRegistry {private values=new Map<number,{runtimeId:string;kind:string}>();register(body:MatterJS.BodyType,runtimeId:string,kind:string){this.values.set(body.id,{runtimeId,kind})}resolve(body:MatterJS.BodyType){return this.values.get(body.id)}remove(body:MatterJS.BodyType){this.values.delete(body.id)}clear(){this.values.clear()}get count(){return this.values.size}}
