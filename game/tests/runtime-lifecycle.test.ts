import test from'node:test';import assert from'node:assert/strict';import{LevelAssetTracker}from'../src/runtime/LevelAssetTracker.ts';import{migrateSave}from'../src/runtime/SaveMigration.ts';
test('level asset references unload only after the last consumer',()=>{const a=new LevelAssetTracker();a.retain('shared');a.retain('shared');assert.equal(a.release('shared'),false);assert.equal(a.release('shared'),true);assert.equal(a.active,0)});
test('legacy saves resolve canonical Tiled identity without arbitrary coordinates',()=>{const saved=migrateSave({nodeId:'node-a',cursor:8},()=> 'runtime-a');assert.deepEqual(saved,{schemaVersion:2,nodeId:'node-a',runtimeLevelId:'runtime-a',checkpointId:null,entryConnectorId:'default',spawnId:'default',floorId:'canonical',elevation:0,cursor:8})});
test('restart lifecycle does not leak tracked assets',()=>{for(let i=0;i<20;i++){const a=new LevelAssetTracker();a.retain(`level-${i}`);assert.equal(a.release(`level-${i}`),true);assert.equal(a.active,0)}});
