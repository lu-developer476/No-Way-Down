import type { LevelWorldDefinition } from './LevelWorldDefinition.ts';
export class LevelTopologySystem { readonly definition:LevelWorldDefinition; constructor(definition:LevelWorldDefinition){this.definition=definition;} validate():boolean{return this.definition.walkableSurfaces.length>0&&this.definition.entryConnectors.every(c=>c.x>=0&&c.x<=this.definition.worldWidth)&&this.definition.exitConnectors.every(c=>c.x>=0&&c.x<=this.definition.worldWidth)&&this.definition.objectiveAnchors.every(a=>a.x>=0&&a.x<=this.definition.worldWidth&&a.y>=0&&a.y<=this.definition.worldHeight);} elevationAt(x:number,y:number):number{return this.definition.walkableSurfaces.find(s=>x>=s.x&&x<=s.x+s.width&&Math.abs(y-s.y)<s.height)?.elevation??this.definition.elevationBase;} }
