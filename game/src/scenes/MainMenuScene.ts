import Phaser from 'phaser';import{CampaignFlow}from'./CampaignFlow';export class MainMenuScene extends Phaser.Scene{constructor(){super('MainMenuScene')}create(){const {width,height}=this.scale;this.add.image(width/2,height/2,'nwd-menu').setDisplaySize(width,height);const start=this.add.text(width/2,height*.82,'NUEVA PARTIDA',{fontFamily:'monospace',fontSize:'24px',color:'#ffffff',backgroundColor:'#111827',padding:{x:20,y:10}}).setOrigin(.5).setInteractive({useHandCursor:true});const run=()=>new CampaignFlow(this).start();start.on('pointerdown',run);this.input.keyboard?.once('keydown-ENTER',run)}}
