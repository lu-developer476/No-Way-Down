import test from'node:test';import assert from'node:assert/strict';import{createHash}from'node:crypto';import{readFileSync}from'node:fs';import{resolve}from'node:path';const repo=resolve(import.meta.dirname,'../..'),m=JSON.parse(readFileSync(resolve(repo,'game/config/protected-assets.json'),'utf8'));
for(const wanted of ['game/public/favicon.png','game/public/assets/images/NWD-menu.png','game/public/assets/images/NWD-characters.png'])test(`${wanted} is protected byte-for-byte`,()=>{const a=m.assets.find((x:{path:string})=>x.path===wanted);assert.ok(a);assert.equal(createHash('sha256').update(readFileSync(resolve(repo,wanted))).digest('hex'),a.sha256)});
test('all versioned environment photographs are protected',()=>{const photos=m.assets.filter((x:{category:string})=>x.category==='environment-photograph');assert.ok(photos.length>=9);for(const a of photos)assert.equal(createHash('sha256').update(readFileSync(resolve(repo,a.path))).digest('hex'),a.sha256)});
