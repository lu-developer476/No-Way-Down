import{readFileSync,readdirSync,statSync}from'node:fs';import{resolve,relative}from'node:path';const root=resolve(import.meta.dirname,'../src');const patterns=[/default\s*:\s*['"]arcade/i,/arcade\s*:/i,/Phaser\.Physics\.Arcade/,/\bthis\.physics\b/,/\bphysics\.add\b/,/\bphysics\.world\b/,/StaticGroup/,/Arcade\.(?:Sprite|Body)/];const failures=[];const walk=d=>{for(const n of readdirSync(d)){const p=resolve(d,n);if(statSync(p).isDirectory())walk(p);else if(/\.ts$/.test(n)){const s=readFileSync(p,'utf8');for(const x of patterns)if(x.test(s))failures.push(`${relative(root,p)}: ${x}`)}}};walk(root);if(failures.length){console.error(failures.join('\n'));process.exit(1)}console.log('Matter-only audit passed: no Arcade production API remains.');
